# FactoryOS Environment Configuration Template
# Copy to .env.development, .env.test, or .env.production as needed.
# DO NOT COMMIT WORKING SECRETS.

# Server Configuration
SERVER_PORT=8080
SPRING_PROFILES_ACTIVE=development
FACTORYOS_TIMEZONE=Asia/Kuala_Lumpur

# PostgreSQL Database
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=factoryos
POSTGRES_USER=factoryos_app
POSTGRES_PASSWORD=replace_with_secure_runtime_password

# Flyway Migration Identity (Separate from runtime)
FACTORYOS_MIGRATION_JDBC_URL=jdbc:postgresql://postgres:5432/factoryos
FACTORYOS_MIGRATION_USER=factoryos_migrator
FACTORYOS_MIGRATION_PASSWORD=replace_with_secure_migrator_password

# JWT Security
# Path to PEM formatted RS256 private/public key files
JWT_PRIVATE_KEY_PATH=/etc/factoryos/keys/jwt_private.pem
JWT_PUBLIC_KEY_PATH=/etc/factoryos/keys/jwt_public.pem
# Fallback secret for local development if keys not mounted
JWT_DEV_SECRET=development_only_secret_key_must_be_at_least_256_bits_long_for_security_12345

# Initial Bootstrap Secret (One-time Admin creation)
FACTORYOS_BOOTSTRAP_ENABLED=true
FACTORYOS_BOOTSTRAP_ADMIN_EMAIL=admin@factoryos.local
FACTORYOS_BOOTSTRAP_ADMIN_PASSWORD=AdminBootstrap2026!Secure

# CORS Allowed Origins
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:80,http://localhost

# Frontend
VITE_API_BASE_URL=/api/v1
